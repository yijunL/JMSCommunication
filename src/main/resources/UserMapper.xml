<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mapper.UserMapper" >

    <sql id="Base_Column_List">
	id,
	username,
	`password`
</sql>
    <resultMap id="BaseResultMap" type="domain.User">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
    </resultMap>
    <insert id="insertUser" parameterType="domain.User">
        insert into user(username,password) value(#{username,jdbcType=CHAR},#{password,jdbcType=CHAR})
    </insert>

    <select id="selectUser"  resultType="domain.User">
        select * from user
    </select>

<!--auto generated by MybatisCodeHelper on 2020-04-11-->
    <select id="findAllByUsername" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
        where username=#{username}
    </select>

<!--auto generated by MybatisCodeHelper on 2020-04-14-->
    <select id="findAllByUsernameAndPassword" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
        where username=#{username} and `password`=#{password}
    </select>

</mapper>
